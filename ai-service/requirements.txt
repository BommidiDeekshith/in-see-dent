# Minimal deps for InSeeDent AI service (Python 3.11+)
# Install in a venv: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
langgraph>=0.2.40,<0.3
langchain-core>=0.3.0,<0.4
pydantic>=2.7.0,<3.0
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.27.0
